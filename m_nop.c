#include "monty.h"

/**
  * _nop -function to do nothing.
  *
  * @stack: head of the stack
  * @line_number: line on command.
  *
  * Return: it returns nothing
  */
void _nop(stack_t **stack, unsigned int line_number)
{

	(void) stack;

	(void) line_number;
}
